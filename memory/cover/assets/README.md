# Cover assets

- референс лица: `victoria.png` (канон имени, не `victoria-sheet.png`)
- `cover_mode=host_reference` — Cover i2i без этого файла не запускать
- если бинарник вложения не попал в checkout: положить owner PNG сюда тем же именем
- не генерировать «похожее» лицо и не подставлять чужой портрет
- `style-refs/` — дополнительные mood refs тенанта (не pink-cat дефолт)
