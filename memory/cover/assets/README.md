# Cover assets — ТАРО СЕЙЧАС

Реф **только Виктории** для i2i. Алёну не класть.
Канон: `shared/cover-host-canon.md`.

## Сейчас: present

Канон в git:

```text
memory/cover/assets/виктория.png
memory/cover/assets/victoria.png   # алиас того же файла
```

`blog-hero.json` → `status: READY`, `assets_status: present`.
`input_urls` = этот файл. Kie GPT Image 2: i2i, `prefer_local_reference` + File Upload.
Публичный URL после заливки — `{{SITE_BASE}}/wp-content/...`. Не catbox, не localhost.

Лицо: длинные прямые светлые волосы, радужка светло-зелёная с лёгким hazel у зрачка (не карие), та же кость.
В промпте не писать `light-brown eyes` — модель красит глаз карим.
Не брюнетка, не двойник, не Алёна, не шов по лицу.
После генерации открыть реф и кадр рядом. Mismatch — пересобрать холст.
Натюрморт без неё — только инлайн, не cover.
Одежду с листа (белый пиджак, карты) не копировать.
Обложку рисует агент. Холл не перерисовывает.
