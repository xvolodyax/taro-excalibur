# style-refs — ТАРО СЕЙЧАС

Чужой коллаж-plate (кот / скотч / неон-розовый) не использовать.

Палитра и запреты — в `memory/cover/cover-design-code.json`  
и `memory/cover/quad-style-taro-seichas.json`.

Лица сюда не класть: лица — в `memory/cover/assets/*.jpg` по `images/refs/README.md`.
