# Cursor Cloud / self-hosted worker
CURSOR_API_KEY=

# Public site
WP_SITE_URL=
WP_ADMIN_URL=
PUBLIC_SITE_URL=

# Excalibur BLOG publish
EXCALIBUR_BLOG_ALLOW_PUBLISH=no
EXCALIBUR_TOPIC_ID=
EXCALIBUR_PROJECT_ROOT=

# Site API publish after GATE PASS (Cursor Cloud Secret → env only).
# First non-empty wins. Never commit a real value.
SITE_PUBLISH_TOKEN=
HALL_PUBLISH_TOKEN=
PUBLISH_TOKEN=
TARO_SITE_TOKEN=

# ---------------------------------------------------------------------------
# SFTP publish credentials (FTP_* names = same SFTP account)
# Transport is ALWAYS SFTP/SSH on port 22 — never plain FTP upload.
# Set these four in Cursor Cloud Secrets; that is enough for publish.
# ---------------------------------------------------------------------------
FTP_HOST=
FTP_USER=
FTP_PASS=
# Alias used by some Cloud providers:
# FTP_PASSWORD=
# SFTP login cwd where wp-load.php lives (canon). Empty or "/" also → "."
FTP_ROOT=.

# Optional SSH_* aliases (same values as FTP_* if you prefer SSH_ names)
# SSH_HOST=
# SSH_PORT=22
# SSH_USER=
# SSH_PASS=
# SSH_PASSWORD=
# SSH_ROOT=.

# MCP tokens via env interpolation in mcp.json — never commit real values
# MCP_KV_TOKEN=

# Yandex Metrica Reports API (content learning / site feedback)
# Docs: https://yandex.ru/dev/metrika/  scope: metrika:read
YANDEX_METRIKA_OAUTH_TOKEN=
YANDEX_METRIKA_COUNTER_ID=

# Optional search-performance loop (read-only; separate from Metrika behavior)
# YANDEX_WEBMASTER_OAUTH_TOKEN=
# YANDEX_WEBMASTER_HOST_ID=

# Yandex Cloud Search API v2 / Wordstat (Scout). Never commit a real value.
YANDEX_CLOUD_SEARCH_API_KEY=
YANDEX_FOLDER_ID=
